> new app logo?
> add app link
> debug anything
> testing
> fix dark mode syncs with device
> remove the difficulty settings and set them permanently. also decide on the settings (need input on this)
> remove all the random extra junk
> make text fit on screens (broken on 12)